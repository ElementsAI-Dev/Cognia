/**
 * Selection store barrel export
 *
 * Re-exports everything from the selection store and types for backward compatibility.
 */
export type {
  SelectionHistoryItem,
  TranslationMemoryEntry,
  SelectionState,
  SelectionActions,
  SelectionStore,
} from './types';
