/**
 * Mock for ink-multi-select package
 */

const React = require('react');

const MultiSelect = ({ items, onSubmit }) => {
  return React.createElement('div', null, 'MultiSelect');
};

module.exports = MultiSelect;
module.exports.default = MultiSelect;
