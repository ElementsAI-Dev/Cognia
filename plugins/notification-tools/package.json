{
  "name": "@cognia/plugin-notification-tools",
  "version": "1.0.0",
  "description": "Notifications and reminders for Cognia AI agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "validate": "cognia-plugin validate --strict",
    "prepublishOnly": "npm run build && npm run validate"
  },
  "devDependencies": {
    "@cognia/plugin-sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": { "@cognia/plugin-sdk": ">=2.0.0" }
}
