{
  "name": "@cognia/plugin-shell-tools",
  "version": "1.0.0",
  "description": "Secure shell command execution and process management for Cognia AI agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "shell-tools": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "validate": "cognia-plugin validate --strict",
    "prepublishOnly": "npm run build && npm run validate"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@cognia/plugin-sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "@cognia/plugin-sdk": ">=2.0.0"
  }
}
